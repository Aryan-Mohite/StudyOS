# StudyOS Frontend — copy this to .env.local

# Point at the Express gateway (never directly at Python AgenticService)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Flip to true to use real backend instead of mock data
NEXT_PUBLIC_USE_REAL_API=true
